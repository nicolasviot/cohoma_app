use core
use gui
use base
use display

import ExclusionArea
_define_
ExclusionAreaLayer (Process map){


	List areas{
/*		ExclusionArea limites(map, "limits")

		ExclusionArea roz_all(map, "roz_all")

		ExclusionArea nfz(map, "nfz")*/
	}
}